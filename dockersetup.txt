docker build -t gspc-feynman .
docker run -it --hostname waas --cap-add=SYS_ADMIN gspc-feynman
docker run -it --hostname $(hostname) --cap-add=SYS_ADMIN gspc-feynman


docker tag gspc-feynman ghcr.io/$(whoami)/gspc-feynman:latest
export GITHUB_TOKEN=your_token_here



echo code_github | docker login ghcr.io -u blociss --password-stdin


GitHub Container Registry (GHCR) using your username blociss.

Now, to push your Docker image to GHCR and make it available for others:

✅ 1. Tag the image correctly (must be lowercase!)
bash

docker tag gspc-feynman ghcr.io/blociss/gspc-feynman:latest
✅ 2. Push the image
bash

docker push ghcr.io/blociss/gspc-feynman:latest
✅ 3. What the other user needs to do (e.g., on macOS or Windows):
Install Docker Desktop

Available for macOS and Windows.

Login to GHCR (optional if image is public)

bash

echo YOUR_TOKEN | docker login ghcr.io -u blociss --password-stdin
Pull and run the image

bash

docker pull ghcr.io/blociss/gspc-feynman:latest
docker run -it --hostname $(hostname) --cap-add=SYS_ADMIN ghcr.io/blociss/gspc-feynman:latest



https://ghcr.io/blociss/gspc-feynman
